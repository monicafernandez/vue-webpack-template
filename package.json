{
  "name": "wiwi-template",
  "description": "A wiwi template + vue-loader setup with hot reload based on the template webpack for quick prototyping.",
  "version": "1.0.0",
  "author": "mfernandez <monica.fernandez@amplia.es>",
  "license": "MIT",
  "scripts": {
    "preversion": "",
    "version": "git add .",
    "postversion": "git checkout master && git pull && git merge develop && git push && git checkout develop && git push && git push --tags"
  },
  "dependencies": {},
  "devDependencies": {}
}
